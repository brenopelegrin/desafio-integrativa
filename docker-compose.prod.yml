
services:
  traefik:
    image: traefik:v2.10
    command:
      - "--api.insecure=false"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
    ports:
      - "8087:80"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
    networks:
      - proxy-tier

  db:
    image: postgres:16-alpine
    env_file: .env.prod
    volumes:
      - pgdata-prod:/var/lib/postgresql/data
    networks:
      - backend-tier
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d gerenciador_processos"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file: .env.prod
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.api.rule=Host(`api.${DEPLOY_EXTERNAL_DOMAIN:-localhost}`)"
      - "traefik.http.routers.api.entrypoints=web"
      - "traefik.http.services.api.loadbalancer.server.port=8080"
      - "traefik.docker.network=desafio-integrativa_proxy-tier"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - proxy-tier
      - backend-tier

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    environment:
      - BACKEND_URL=http://api:8080
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.frontend.rule=Host(`${DEPLOY_EXTERNAL_DOMAIN:-localhost}`)"
      - "traefik.http.routers.frontend.entrypoints=web"
      - "traefik.http.services.frontend.loadbalancer.server.port=80"
      - "traefik.docker.network=desafio-integrativa_proxy-tier"
    networks:
      - proxy-tier

networks:
  proxy-tier:
  backend-tier:

volumes:
  pgdata-prod:
